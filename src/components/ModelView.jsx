import { View } from "@react-three/drei";

function ModelView({
  index,
  groupRef,
  gsapType,
  controlRef,
  setRotationState,
  size,
  item,
}) {
  return <div>ModelView</div>;
}

export default ModelView;
